# @openweave/pen

Pencil.dev `.pen` document parser and SceneGraph adapter.

This package owns the `.pen` document model and import policy. It depends on `@openweave/scene-graph` for the shared editable design model and does not depend on `@openweave/core`.
